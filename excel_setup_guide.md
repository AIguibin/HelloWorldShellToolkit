# Excel提示管理系统设置指南

## 文件说明

已生成以下文件：
- `prompt_management_template.csv` - 使用说明工作表
- `prompt_management_data.csv` - 主数据表（包含示例数据）

## Excel设置步骤

### 步骤1：创建新Excel工作簿
1. 打开Microsoft Excel
2. 创建新工作簿
3. 将工作表重命名为：
   - Sheet1 → "使用说明"
   - Sheet2 → "提示管理"

### 步骤2：导入数据
1. 打开"使用说明"工作表
2. 导入 `prompt_management_template.csv` 文件
3. 打开"提示管理"工作表
4. 导入 `prompt_management_data.csv` 文件

### 步骤3：设置列宽和格式

#### 列宽建议：
- A列 (Prompt ID): 15字符
- B列 (Prompt Type): 12字符
- C列 (Prompt Content): 50字符
- D列 (Application Scenario): 25字符
- E列 (Creator): 20字符
- F列 (Creation Date): 12字符
- G列 (Last Modified Date): 12字符
- H列 (Status): 10字符
- I列 (Remarks): 30字符

#### 格式设置：
- 标题行：加粗，背景色浅蓝色
- 日期列：设置为日期格式 YYYY-MM-DD
- 文本列：自动换行

### 步骤4：设置数据验证

#### Prompt Type列数据验证：
1. 选择B列（从B2开始）
2. 数据 → 数据验证 → 设置
3. 允许：列表
4. 来源：`technical,business`
5. 确定

#### Status列数据验证：
1. 选择H列（从H2开始）
2. 数据 → 数据验证 → 设置
3. 允许：列表
4. 来源：`draft,reviewed,approved,retired`
5. 确定

### 步骤5：设置条件格式

#### 根据状态设置颜色：
1. 选择H列（从H2开始）
2. 开始 → 条件格式 → 新建规则
3. 使用公式确定要设置格式的单元格
4. 设置以下规则：

**draft状态（黄色）**
- 公式：`=$H2="draft"`
- 格式：填充黄色

**reviewed状态（蓝色）**
- 公式：`=$H2="reviewed"`
- 格式：填充浅蓝色

**approved状态（绿色）**
- 公式：`=$H2="approved"`
- 格式：填充浅绿色

**retired状态（灰色）**
- 公式：`=$H2="retired"`
- 格式：填充浅灰色

### 步骤6：冻结窗格
1. 在"提示管理"工作表中
2. 选择A2单元格
3. 视图 → 冻结窗格 → 冻结首行

### 步骤7：设置自动筛选
1. 选择标题行（A1:I1）
2. 数据 → 筛选

## 数据录入规范

### Prompt ID生成规则：
- 格式：P-YYYYMMDD-XXX
- 示例：P-20260116-001
- 建议使用Excel公式自动生成：
```excel
="P-"&TEXT(TODAY(),"YYYYMMDD")&"-"&TEXT(ROW()-1,"000")
```

### 日期自动填充：
- Creation Date：使用 `=TODAY()` 或手动输入
- Last Modified Date：可使用公式自动更新

### 数据完整性检查：
1. 所有必填字段必须填写
2. Prompt ID必须唯一
3. 日期格式必须正确

## 高级功能（可选）

### 数据透视表：
1. 选择数据区域
2. 插入 → 数据透视表
3. 可用于统计不同类型提示的数量

### 图表分析：
1. 基于状态创建饼图
2. 基于创建日期创建趋势图

## 维护建议

1. **定期备份**：建议每周备份Excel文件
2. **数据清理**：定期检查retired状态的记录
3. **权限管理**：设置不同用户的操作权限
4. **版本控制**：重要变更时保存文件版本

## 故障排除

### 常见问题：
1. **数据验证不工作**：检查单元格格式是否为文本
2. **条件格式不显示**：检查规则顺序和公式正确性
3. **日期格式错误**：重新设置单元格格式为日期

### 性能优化：
1. 数据量较大时，考虑使用Excel表格功能
2. 定期清理不需要的格式和公式
3. 使用分页显示大量数据

---

**注意**：本指南基于Microsoft Excel 2016及以上版本编写，不同版本界面可能略有差异。